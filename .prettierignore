node_modules/
dist/
coverage/
# Local OMX (Claude Code) runtime state — never repo content; untracked files here should not
# make `pnpm format:check` fail for contributors who happen to have this local tool installed.
.omx/
# Next-generated, "should not be edited" (see the file's own header comment) — `next build`
# rewrites its quote style on every build, which would otherwise make `format:check` flap.
packages/web/next-env.d.ts
